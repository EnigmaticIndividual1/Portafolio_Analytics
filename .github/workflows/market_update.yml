name: Update Market Snapshot

on:
  schedule:
    # 7:30–7:59 AM Sonora (UTC-7)
    - cron: "30-59/5 14 * * 1-5"
    # 8:00 AM – 2:59 PM Sonora
    - cron: "*/5 15-21 * * 1-5"
    # 3:00 PM Sonora (última corrida)
    - cron: "0 22 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Update historical P/L
        run: python scripts/update_historical_pnl.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git add data/historical_pnl.tsv
          git commit -m "Automated update of historical market data"
          git push
